# Hard-case overlays pending

Run the CUDA teacher-mining command in `docs/TOOTH_V2_GPU_HANDOFF.md`. It uses only the V2 train manifest and writes derived overlays here without modifying source OPGs.

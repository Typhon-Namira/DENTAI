# Tooth V1 versus V2

Comparison is pending V2 training and evaluation on identical compatible locked sets. No V2 performance values exist, and V2 is not declared superior. The evaluator must report bbox mAP50/mAP50:95, mask AP, precision, recall, IoU, Dice, misses and false positives per image, latency, RAM/VRAM, model size, and hard-case subsets.

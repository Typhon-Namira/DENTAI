FROM python:3.12-slim
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1 PIP_DISABLE_PIP_VERSION_CHECK=1
WORKDIR /app
COPY radar_collector_service/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY radar_collector_service ./radar_collector_service
RUN useradd --create-home appuser && chown -R appuser:appuser /app
USER appuser
EXPOSE 8080
CMD ["sh", "-c", "uvicorn radar_collector_service.app:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1"]

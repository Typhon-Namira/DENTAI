# Preferred local-browser setup: keep browser requests same-origin and let Vite
# proxy them to the remote DENTAI backend. Do not add /api/v1 to the target.
VITE_DENTAI_API_BASE_URL=
DENTAI_PROXY_TARGET=https://your-dentai-production-domain.example
